import React from 'react';

/**
 * ThreeDPageTransition (Disabled)
 * Page swipe navigation and slide transitions have been completely removed as requested.
 */
const ThreeDPageTransition = ({ children }) => {
  return <>{children}</>;
};

export default ThreeDPageTransition;
