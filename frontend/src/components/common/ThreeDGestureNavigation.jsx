import React from 'react';

/**
 * ThreeDGestureNavigation (Disabled)
 * Page swipe navigation feature has been completely removed as requested.
 */
const ThreeDGestureNavigation = ({ children }) => {
  return <>{children}</>;
};

export default ThreeDGestureNavigation;
