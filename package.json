{
  "name": "mhp-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && npm start",
    "dev": "cd frontend && npm run dev"
  }
}
